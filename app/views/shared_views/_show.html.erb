<% recordatorios = current_user.recordatorios %>
<div class="well">
	<ul>
		<% recordatorios.each do |r| %>
		<li><%= link_to "ø", edit_recordatorio_path(r) %>-<%= r.fecha_prestamo %> | <%= r.fecha_entrega %> | <%= r.contacto %> | <%= r.concepto %> | <%= r.detalle %> <% if r.regresado then %> OK <% else %> Not OK <% end %> </li>
		<% end %>
	</ul>
	
</div>